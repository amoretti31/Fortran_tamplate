# 📦 Fortran Template

A **starter template for modern Fortran projects** with Docker-based CI testing, clean project structure, and easy local development.

---

## ✅ Features

- Dockerized build environment with `gfortran` and essential tools  
- Clean project layout: `src/`, `app/`, `tests/`  
- Unit test support with formatted outputs  
- GitHub Actions CI workflow with readable test summary  
- Optional artifact upload for test logs  

---

## 📁 Project Structure

```markdown
Fortran_tamplate/
├── src/          # Source code directory
├── tests/        # Unit tests and test cases
├── input/        # Input files for the application
├── output/       # Output files generated by the application
├── Docker/       # Docker-related files
├── .github/workflows/  # GitHub Actions workflows for CI/CD
├── .devcontainer/      # Development container configuration files
```


---

## 🚀 Quick Start

### 1️⃣ Build the Docker Image

```bash
docker build -t fortran-template Docker
```


### 2️⃣ Compile a Fortran file locally

```bash
docker run --rm \
    -v "$PWD/src":/workspace \
    -w /workspace \
    fortran-template \
    gfortran main.f90 -o main

```



### 3️⃣ Run an interactive shell


```bash
docker run -it --rm \
    -v "$PWD":/workspace \
    -w /workspace \
    fortran-template \
    /bin/bash

```

### 4️⃣ Run Tests


```bash
docker run --rm \
    -v "$PWD":/work \
    -w /work \
    fortran-template \
    bash Docker/build_test.sh

```


---
## 🧩 Build a New Module and Test

### 1️⃣ Create a New Module

1. Navigate to the `src/` directory

2. Create a new Fortran module file, e.g., `math_utils.f90`

3. Navigate to the `tests/` directory

4. Create a new test file, e.g., `test_math_utils.f90`
  
5. Build the module and test program
---



## 🛠️ Recommended Workflows and Best Practices

### 🧹 Maintain Clean Code
- Use consistent formatting and indentation in your Fortran code.
- Add comments to explain complex logic or calculations.
- Avoid hardcoding values; use parameters or input files instead.

### 🔄 Version Control
- Commit changes frequently with clear and descriptive messages.
- Use feature branches for new functionality or bug fixes.
- Regularly merge or rebase to keep your branch up-to-date with `main`.

### 🧪 Test Early and Often
- Write unit tests for all critical functions and modules.
- Run tests locally before pushing changes to the repository.
- Use the CI pipeline to ensure all tests pass on every commit.

### 📦 Docker Usage
- Use the provided Docker image for consistent builds across environments.
- Rebuild the Docker image periodically to ensure dependencies are up-to-date.
- Avoid installing additional tools inside the container unless necessary.

### 📂 Organize Your Project
- Keep source files in the `src/` directory and tests in `tests/`.
- Use the `input/` and `output/` directories for data files to maintain a clean workspace.
- Document any changes to the project structure in the `README.md`.

### 📜 Documentation
- Update the `README.md` file whenever you add new features or workflows.
- Include examples and usage instructions for new functionality.
- Use inline comments and external documentation to make the project easier to understand.

### 🔒 Security
- Avoid committing sensitive data or credentials to the repository.
- Use `.gitignore` to exclude unnecessary or sensitive files from version control.
- Regularly review dependencies for vulnerabilities.

--- 